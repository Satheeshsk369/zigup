# The TUI

- The project previously use the tui based installation, but later realized i need to do more complicated work to achieve those. that also not contribute to the simplifying the app function. so i withdraw the idea from this project. And zigup achieves the simplicity in configuration not in the user interface.
- But during the exploration i observed something, the interactive ui does not always need the keybinding. we can also achieve more in stdio/stdout itself. let we explore this in a another project.
- And the ADT is helpful wherever the type composition is needed. but need to fix it boundary to making only useful compositions. 
